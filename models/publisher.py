class Publisher:
    def __init__(self, name, existing, id = None):
        self.name = name
        self.existing = existing #boolean
        self.id = id

#Need to figure out the best way to integrate the boolean.
